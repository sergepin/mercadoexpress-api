# Copiar este archivo a .env antes de correr el proyecto (docker-compose lo lee automáticamente)

DB_HOST=localhost
DB_PORT=5432
DB_USER=inventory_user
DB_PASSWORD=inventory_pass
DB_NAME=inventory_db

PORT=3000
